#main-nav
  nav.navbar.navbar-default
    .container
      link-to "dockworker" class="navbar-brand"
        span.capitalize.bold dock worker

      ul.nav.navbar-nav.navbar-right
        li
          a.pointer click="exitRole"
            i.fa.fa-sign-out
#scalemaster-application.dockworker-application
  section#scale-info
    .container
      .row
        .col-xs-12
          .btn-group.btn-group-lg
            link-to "dockworker.index" class="btn btn-default"
              i.fa.fa-dashboard
              span.fa.nav-title dash
            link-to "dockworker.status" class="btn btn-default"
              i.fa.fa-camera
              span.fa.nav-title status
            link-to "dockworker.inventory" class="btn btn-default"
              i.fa.fa-cubes
              span.fa.nav-title inventory
      .row
        == outlet

  footer#scale-footer
    .container
      .row
        .col-xs-12
          h4.footer-title
            span.fa dock worker